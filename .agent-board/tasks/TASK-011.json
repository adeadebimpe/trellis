{
  "id": "TASK-011",
  "title": "Derive short task titles from entry with ellipsis and drawer tooltip",
  "status": "ready-for-agent",
  "priority": "medium",
  "assignedAgent": "claude",
  "qaAgent": "unassigned",
  "brief": "Create short titles based on entrry for new task, and titles should fit using ellipsis and maybe a tooltip, if it opens in the sidebar",
  "description": "When a user creates a new task from the New task composer, the raw entry text currently becomes the title source and long entries produce awkward, mid-word-clipped titles (e.g. 'short titles based on entrry for new task, and titles should fit usin...'). This task improves title generation and title display.\n\n1) Title derivation: when a task is created from the composer brief, derive a concise title from the entry text — take the first meaningful line, strip leading imperative verbs (build/create/add, as deriveTaskTitle in src/prdPrompt.ts already does), strip trailing punctuation, and clip to a short length (target ~60 characters max) at a word boundary rather than mid-word, appending a single ellipsis character '…' when clipped. The full entry remains untouched in the task's brief field; only the display title is shortened. AI-generated titles from the PRD flow (extractTitlePatch in src/prdPrompt.ts) should use the same word-boundary clipping helper instead of the current slice(0,69) mid-word cut.\n\n2) Display fit: on board cards (.cardTitle in webview/styles.css, rendered at webview/main.tsx line ~375), long titles must truncate with CSS ellipsis (single line or two-line clamp — follow the existing card visual style) instead of wrapping indefinitely, and the card element must carry a title attribute (native tooltip) with the full title text.\n\n3) Sidebar/drawer: when a task opens in the drawer (the right-hand sidebar, .drawer in webview/styles.css), the title input (.titleInput, webview/main.tsx line ~430) must show ellipsis truncation for overflowing titles when not being edited, and expose the full title via a title attribute tooltip on hover. Editing behavior must be unaffected: focusing the input shows and edits the full value.\n\nAssumption to record in agentNotes: 'sidebar' in the brief refers to the existing task drawer opened from a card, since that is the only sidebar-like surface in the webview.",
  "acceptanceCriteria": [
    "Creating a new task from the composer with a long entry produces a title of at most ~60 characters, clipped at a word boundary with a trailing '…', never cutting a word in half",
    "Creating a task with a short entry (under the limit) uses the cleaned entry as-is with no ellipsis appended",
    "The full original entry text is preserved in the task brief and is still used as the PRD source (getPrdSourceBrief behavior unchanged)",
    "deriveTaskTitle and the AI title patch path (extractTitlePatch) share the same word-boundary clipping logic — no remaining mid-word slice(0,69) truncation",
    "Board card titles truncate with an ellipsis instead of overflowing or wrapping beyond the card's visual limit, and hovering a truncated card title shows the full title via a native tooltip (title attribute)",
    "In the task drawer, an overflowing title in the title input renders with ellipsis when unfocused, and hovering it shows the full title as a tooltip",
    "Focusing the drawer title input allows viewing and editing the complete title text normally",
    "Existing tasks with saved titles are unaffected except for display truncation — no stored titles are rewritten on load beyond current behavior",
    "Unit tests in tests/prdPrompt.test.mjs cover word-boundary clipping: long entry, entry exactly at the limit, entry with a very long first word, and multi-line entry using only the first line"
  ],
  "qaChecklist": [
    "Create a task with a 200+ character single-line entry and verify the generated title ends at a word boundary with '…' and is ≤ ~60 characters",
    "Create a task with a multi-line entry and verify only the first non-empty line feeds the title",
    "Create a task whose entry starts with 'Create ' or 'Build ' and verify the verb is stripped from the title",
    "Open a task with a long title in the drawer and confirm the title shows ellipsis when unfocused and the full text on focus",
    "Hover the truncated title on a board card and in the drawer and confirm a tooltip shows the complete title",
    "Verify the brief field in .agent-board/tasks/<id>.json still contains the full original entry",
    "Run Generate PRD on a task and confirm the AI-supplied title is also word-boundary clipped, not cut mid-word",
    "Edit a title manually in the drawer to something long, save, and confirm the card truncates it correctly without altering the stored value"
  ],
  "designQaChecklist": [
    "Card title truncation matches the existing card typography (13px, weight 500) and does not change card height rhythm",
    "Ellipsis uses the single '…' character, not three periods, in generated titles",
    "Drawer title input keeps its 17px semibold styling; truncation only appears when the text overflows",
    "Tooltip appears only when the title is actually truncated where feasible, or is harmless (identical text) when not",
    "No layout shift in the drawer header or card when toggling between truncated and focused/full states"
  ],
  "validationCommands": [
    "npm run typecheck",
    "npm run compile",
    "npm run test",
    "npm run build"
  ],
  "relevantFiles": [
    "src/prdPrompt.ts",
    "src/storage.ts",
    "src/extension.ts",
    "webview/main.tsx",
    "webview/styles.css",
    "src/types.ts",
    "tests/prdPrompt.test.mjs"
  ],
  "constraints": [
    "Follow existing project patterns before introducing new abstractions — extend deriveTaskTitle/extractTitlePatch in src/prdPrompt.ts rather than adding a new title module",
    "Keep changes scoped to title derivation and title display; do not modify PRD generation content, task schema, or unrelated drawer fields",
    "Do not overwrite unrelated user or agent changes; do not rewrite stored titles of existing tasks",
    "Do not alter the brief field — it is the PRD source of truth",
    "Use the CSS variables and class conventions already present in webview/styles.css; no new styling frameworks",
    "VS Code webview context: rely on native title-attribute tooltips (window.confirm-style APIs are blocked; custom tooltip libraries are unnecessary)"
  ],
  "agentNotes": "",
  "qaNotes": [],
  "qaEvidence": [],
  "activityLog": [
    {
      "timestamp": "2026-07-17T20:59:19.415Z",
      "actor": "vscode",
      "message": "Created blank task."
    },
    {
      "timestamp": "2026-07-17T21:00:25.439Z",
      "actor": "claude-code",
      "message": "Generated PRD."
    },
    {
      "timestamp": "2026-07-17T21:00:39.900Z",
      "actor": "vscode",
      "message": "Updated task."
    }
  ],
  "claimedBy": "",
  "qaClaimedBy": "",
  "branchName": "",
  "worktreePath": "",
  "claimedAt": "",
  "lastValidation": null,
  "shipResult": null,
  "lastUpdated": "2026-07-17T21:00:39.900Z"
}
